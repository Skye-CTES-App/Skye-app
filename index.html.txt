<!DOCTYPE html>

<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<title>Skye — Calm Through Every Storm</title>

<link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@300;400;500;600;700&family=DM+Serif+Display:ital@0;1&display=swap" rel="stylesheet">

<style>
*{
  box-sizing:border-box;
  margin:0;
  padding:0;
  -webkit-tap-highlight-color:transparent;
}

:root{
  --bg:#0B1520;
  --card:#142231;
  --card2:#1a2d40;

  --teal:#2DC88A;
  --blue:#6AAFE0;
  --yellow:#FFD100;
  --pink:#C0386A;

  --white:#FFFFFF;
  --off:#C8DCF0;
  --muted:#6A8BA8;
  --dim:#3A5570;

  --safe-bg:rgba(45,200,138,0.12);
  --safe-border:rgba(45,200,138,0.3);
}

html,body{
  height:100%;
  overflow:hidden;
}

body{
  background:var(--bg);
  color:var(--off);
  font-family:'DM Sans',sans-serif;
}

/* ---------- MAIN APP ---------- */

.app{
  height:100%;
  overflow:auto;
}

/* ---------- TOOL CARDS ---------- */

.card{
  background:var(--card);
  border-radius:18px;
  padding:18px;
  margin:14px;
  cursor:pointer;
  transition:0.25s ease;
}

.card:hover{
  transform:scale(1.02);
}

/* ---------- OVERLAY SYSTEM (FIXED) ---------- */

.overlay{
  position:fixed;
  inset:0;
  background:var(--bg);
  z-index:9999;

  opacity:0;
  pointer-events:none;
  transform:translateY(30px);
  transition:.35s ease;
}

.overlay.active{
  opacity:1;
  pointer-events:auto;
  transform:translateY(0);
}

.overlay-content{
  height:100%;
  overflow:auto;
  padding:20px;
}

/* Prevent background scrolling */
body.locked{
  overflow:hidden;
}

/* ---------- BREATHING CIRCLE ---------- */

.breath-circle{
  width:260px;
  height:260px;
  border-radius:50%;
  border:2px solid var(--teal);
  margin:60px auto;
  display:flex;
  align-items:center;
  justify-content:center;
  font-size:28px;
  opacity:.8;
  transition:transform 4s ease;
}

.breath-circle.inhale{
  transform:scale(1.25);
}

.breath-circle.exhale{
  transform:scale(1);
}

/* ---------- BUTTON ---------- */

.primary-btn{
  background:white;
  color:black;
  border:none;
  padding:16px 28px;
  border-radius:30px;
  font-weight:600;
  font-size:18px;
  display:block;
  margin:20px auto;
}

/* ---------- NAV ---------- */

.nav{
  position:fixed;
  bottom:0;
  left:0;
  right:0;
  background:#08121b;
  padding:14px;
  display:flex;
  justify-content:space-around;
}
</style>
</head>

<body>

<div class="app">

  <div class="card" onclick="openOverlay('breathing')">
    <h3>Breathing Timer</h3>
    <p>Box breathing • 60 seconds</p>
  </div>

  <div class="card" onclick="openOverlay('grounding')">
    <h3>5-4-3-2-1 Grounding</h3>
    <p>Sensory anchoring</p>
  </div>

</div>

<!-- BREATHING OVERLAY -->
<div id="breathing" class="overlay">
  <div class="overlay-content">

    <button onclick="closeOverlay()">← Back</button>

    <div id="circle" class="breath-circle">
      Ready
    </div>

    <button class="primary-btn" onclick="startBreathing()">
      Begin
    </button>

  </div>
</div>

<!-- GROUNDING OVERLAY -->
<div id="grounding" class="overlay">
  <div class="overlay-content">

    <button onclick="closeOverlay()">← Back</button>

    <h2>Look around you.</h2>
    <p>Name five things you can see.</p>

  </div>
</div>

<div class="nav">
  <div>Now</div>
  <div>Toolkit</div>
  <div>Profile</div>
  <div>History</div>
</div>

<script>

/* ---------- OVERLAY CONTROL ---------- */

function openOverlay(id){
  document.body.classList.add("locked");
  document.getElementById(id).classList.add("active");
}

function closeOverlay(){
  document.body.classList.remove("locked");
  document
    .querySelectorAll(".overlay")
    .forEach(o=>o.classList.remove("active"));
}

/* ---------- BREATHING TIMER ---------- */

let breathingInterval;

function startBreathing(){
  const circle=document.getElementById("circle");
  let inhale=true;

  clearInterval(breathingInterval);

  breathingInterval=setInterval(()=>{
    if(inhale){
      circle.textContent="Inhale";
      circle.classList.add("inhale");
      circle.classList.remove("exhale");
    }else{
      circle.textContent="Exhale";
      circle.classList.add("exhale");
      circle.classList.remove("inhale");
    }
    inhale=!inhale;
  },4000);
}

</script>

</body>
</html>